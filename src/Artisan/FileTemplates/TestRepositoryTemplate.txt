import %name%Interface from '../../app/Repositories/%name%Interface';

export default class %name% implements %name%Interface {
    /**
     * Example
     */
    public async example(limit: number): Promise<any>
    {
        return 42;
    }
}
